machine:
  timezone: Europe/Berlin

test:
  pre:
    - ./gradlew projectInfo
  override:
    - ./gradlew clean test -p player
  post:
    - ./gradlew cobertura coveralls

deployment:
  production:
    branch: master
    commands:
      # Publish to BINTRAY
      - ./gradlew bintrayUpload
